spring.application.name=phamhuynhtai

# ==============================================
# Docker Compose Integration
# ==============================================
# Tắt tự động khởi động Docker Compose khi chạy app local (không cần Docker Desktop)
# Đổi thành true nếu muốn Spring Boot tự quản lý Docker Compose
spring.docker.compose.enabled=false

# ==============================================
# Datasource (khi chạy không qua Docker)
# ==============================================
# Bỏ comment và điền thông tin DB local của bạn nếu muốn kết nối DB
# spring.datasource.url=jdbc:mysql://localhost:3306/phamhuynhtai?useSSL=false&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=root
# spring.jpa.hibernate.ddl-auto=update
